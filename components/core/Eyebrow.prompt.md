Mono uppercase kicker label above section headings — the signature Juris8 eyebrow with a leading violet dot.

```jsx
<Eyebrow>Como funciona</Eyebrow>
<h2 className="sec-h2">3 passos para <span className="grad">começar a monitorar.</span></h2>
```

Always pairs above an `<h2>`. Keep the text short (1–3 words). Color is fixed to the violet brand accent.
